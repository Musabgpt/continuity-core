{
  "schema_version": 1,
  "timestamp": "2026-09-24T05:36:00Z",
  "status": "verified",
  "scope": "machine-readable-handoff-determinism",
  "implementation_commit": "e911ef65f99dbf3ffdcb3f4b76feee79381469eb",
  "changes": [
    "add regression proving handoff JSON is invariant to state/event timestamps",
    "assert handoff excludes updated_at, event ts, and transaction txid by parsed field names",
    "assert handoff uses the canonical compact sorted JSON representation",
    "restore the original configured project root after the isolated fixture"
  ],
  "verification": {
    "run": 35956570395,
    "conclusion": "success",
    "sequence": [
      "python -m unittest discover -s tests -v",
      "python continuity.py validate",
      "python continuity_validate_json.py --root .",
      "python continuity_integrity.py --audit-all --root ."
    ]
  },
  "regression_history": {
    "failed_run": 35956481433,
    "failure": "New contract used a substring assertion for ts, which matched the letters ts inside status.",
    "correction": "Changed the assertion to inspect parsed handoff field names; no production handoff behavior change was required."
  },
  "next_action": "Select the next highest-signal deterministic continuity gap after the verified handoff boundary."
}
